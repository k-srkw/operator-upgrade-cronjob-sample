apiVersion: v1
kind: ConfigMap
metadata:
  name: installplan-approver-allowlist
  namespace: operator-upgrade-approver
  labels:
    app.kubernetes.io/name: installplan-approver
    app.kubernetes.io/part-of: operator-upgrade-approver
data:
  # 自動承認の対象とする Subscription を列挙する。
  # 1 行 1 件、書式: <namespace>/<subscription-name>
  # `#` 始まりの行と空行は無視される。
  #
  # 例:
  #   openshift-logging/cluster-logging
  #   openshift-operators/grafana-operator
  allowlist: |
    # TODO: 自動承認対象の Subscription を追加すること
